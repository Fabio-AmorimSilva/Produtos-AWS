name: Deploy ASP.NET Core Produtos-AWS tp Elastic Beanstalk
on:
  push:
    branches:
      - main

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  APPLICATION_NAME: ${{ secrets.EB_APPLICATION_NAME }}
  ENVIRONMENT_NAME: ${{ secrets.EB_ENVIRONMENT_NAME }}
  S3_BUCKET: ${{ secrets.S3_BUCKET_DEPLOY }}

jobs:
  deploy:
    runs-on: ubuntu-lastest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configurar .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build project
        run: dotnet build --configuration Release --no-restore

      - name: Publish project
        run: dotnet publish ./ProdutosAws.Web/ProdutosAws.Web.csproj -c Release -o published --no-build

      - name: Zip published files
        run: cd published && zip -r ../deploy.zip *

      - name: Configurar Credenciais AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload para S3
        run: aws s3 cp deploy.zip s3://${{ env.S3_BUCKET_DEPLOY }}/deploy-${{ github.sha }}.zip

      - name: Criar nova versão para Elastic Beanstalk
        run: |
          aws elasticbeanstalk create-application-version \
            --application-name ${{ env.EB_APPLICATION_NAME }} \
            --version-label v-${{ github.sha }} \
            --source-bundle S3Bucket=${{ env.S3_BUCKET_DEPLOY }},S3Key=deploy-${{ github.sha }}.zip

      - name: Deploy para Elastic Beanstalk
        run: |
          aws elasticbeanstalk update-environment \
            --environment-name ${{ env.EB_ENVIRONMENT_NAME }} \
            --version-label v-${{ github.sha }}